version: '3.8'
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  api:
    build:../api
    ports:
      - "8080:8080"
    environment:
      REDIS_URL: redis:6379
      PORT: 8080
    depends_on:
      - redis
